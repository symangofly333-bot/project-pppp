# PROJECT — ChatGPT 초보자용 가이드 확장 프로그램

> **이 문서가 유일한 기준 문서다.** 기존 `PROJECT_HANDOFF.md` / `ROADMAP.md` / `CONTEXT.md` / `MARKET_FINDINGS.md`는 이 문서로 통합되었으며 폐기한다.
> 역할 지시는 별도 문서 `AI_ROLE_PROMPT.md` 참고.

---

## 0. 읽는 사람에게

- 사용자는 **코딩을 전혀 모르는 완전 초보**다. 전문 용어는 반드시 쉬운 비유로 풀어서 설명할 것.
- 코드를 짜거나 바꾸면 **바뀐 부분만** 발췌해서 보여줄 것.
- 실제로 파일을 만들기 전에 **계획을 먼저 설명하고 승인**을 받을 것.
- 사용자는 직설적이며, 틀린 지적은 강하게 반박한다. 반박당하면 방어하지 말고 검토하되, 근거가 있으면 물러서지도 말 것.
- **큰 변화(설계 결정, 방향 전환, 새 확정 사항)가 생기면 이 PROJECT.md를 즉시 업데이트하고 git에도 커밋할 것.**
- 현재 Claude Sonnet과 Opus를 함께 쓴다. Opus는 토큰 소비가 많지만 복잡하거나 중요한 작업에 더 적합하다 — **Opus로 전환할 상황이 오면 출력 마지막에 짧게 언급할 것.**

---

## 1. 프로젝트 정의

### 한 줄 목적 (2026-07-30 확정 갱신 — 1-3 참고)

**~~코딩을 전혀 모르는 완전 초보가, ChatGPT를 처음 켰을 때 "뭘 눌러야 할지 몰라서" 포기하지 않게 하는 것.~~** (옛 문장. "기존 ChatGPT 화면을 도와준다"는 전제가 1-3에서 깨져서 폐기)

**AI(ChatGPT)를 어떻게 써야 할지 모르는 사람이, AI와 직접 상호작용하며 자연스럽게 활용법을
익히게 하는 것.**

**미션은 그 자체가 목적이 아니라 매개다** — 상호작용을 이끌어내는 수단. 진짜 가치는 "미션을
깼다"가 아니라 "AI와 실제로 주고받아봤다"에 있다. 미션 관련 작업이 이 상호작용을 늘리는 데
기여하지 않으면 후순위다. (배우기용 AI·팝업 AI가 내비게이션 최상단에 있고 미션이 `More` 안에
있는 것도 이 원칙에서 나온 결정 — 1-2 참고)

이 문장에서 벗어나는 작업이 생기면 즉시 지적할 것. 기술적으로 훌륭해도 이 목적에 기여하지 않으면 후순위다.

### 제품 형태 (2026-07-30 갱신 — 아래 1-1, 1-3, 1-4 참고)

**본체는 COPEN 자체 웹(미션 시스템)이다. 크롬 확장은 폐기 결정(1-4 참고).**

| | 무엇 | 역할 |
|---|---|---|
| **크롬 확장** (chatgpt.com) | 가이드 오버레이 — 실제 버튼을 화살표로 가리키며 ChatGPT 화면 익히기 (AI 없음) | ❌ **폐기 결정(1-4).** 설치 장벽이 크고 DOM 스크래핑이라 깨지기 쉬움. 코드는 git에 남겨두되 더 개발 안 함 |
| **COPEN 웹** | 미션을 하나씩 깨며 AI와 상호작용하는 곳 | **제품의 본체.** 미착수 |
| **배우기용 AI / 팝업 AI** | 사용자가 물어보면 답하는 AI (자연어, `PROMPTS.md` 1·2절) | 최상단 핵심 기능(1-2 참고). 서버(`server/ask-claude.js`)와 프롬프트는 UI 무관하게 재사용 가능 — 웹에 새 프론트만 붙이면 됨 |

### 핵심 비유
이케아 조립설명서. 사용자가 자기 페이스대로 [확인] 버튼을 눌러 다음 단계로 넘어간다. AI가 사용자의 행동을 실시간으로 감시하지 않는다.

### 1-1. ⚠️ 이 갱신으로 재검토가 필요해진 것

2026-07-30까지 이 문서는 **"크롬 확장이 제품의 본체"** 라고 적혀 있었다. 실제 구상은 그게 아니라
**COPEN 웹이 본체이고 확장은 선택적 입문**이었는데, 문서에 반영된 적이 없었다.
그동안의 모든 결정(2절 타겟, 3절 경쟁, 6절 리스크, 8절 로드맵)은 **옛 전제 위에서 내려진 것**이므로
그대로 믿으면 안 된다. 특히:

- **8절 로드맵**에는 COPEN 웹이 아예 없다. Phase 3~5는 전부 확장 프로그램 기준이다. **미착수.**

**2절 타겟 결론은 재확인 후 유지 — 리포트 3이 아니라 여전히 리포트 5 계열이다.**
아래에서 다시 정리한다.

### 1-2. 타겟과 차별점 재확인 (2026-07-30)

**타겟:** AI를 어떻게 써야 할지 모르는 사람. **리포트 3(능동적 학습자, 스스로 자료를 찾아다니는
사람)은 명시적으로 타깃이 아니다.** 리포트 5(수동적) 계열에 가깝다. 여기에 "기초 코딩을 배우고
싶은 사람(바이브 코더)"도 포함된다 — 단, 코딩 미션은 아직 없다(1-1 참고).

**리포트 5와의 표면적 모순, 그리고 해소:** 리포트 5의 정의는 "AI 학습 사이트를 전혀 안 감"이다.
COPEN 웹 자체가 학습 사이트인데 어떻게 이 사람들이 오는가? 답은 **자발적으로 찾아오는 게
아니라는 것.** 흐름은:

```
마케팅(Product Hunt → 이후 광고)
  → 크롬 확장(이미 있는 ChatGPT 화면 안에서 마주침, 선택적 튜토리얼)
    → COPEN 웹(미션)으로 넘어감
```

확장은 "찾아오게" 만드는 장치가 아니라 "이미 발 들인 사람을 다음 단계로 넘겨주는" 장치다.
이 구조라면 타겟이 수동적이어도 성립한다. (초기 유입 채널인 Product Hunt 자체는 기술에 밝은
얼리어답터가 많이 보는 곳이라 타겟과 안 맞을 수 있다는 지적이 있었으나, 이건 GTM/마케팅
단계에서 다룰 문제로 분리 — 지금 제품 설계와는 별개.)

**차별점:** 기존 전자책·영상·유료 강의의 3가지 문제 — (1) 시작 난이도가 사람마다 다름
(2) 자료가 너무 많아 뭘 봐야 할지 모름 (3) 사용자가 능동적으로 배우려는 의지가 있어야 함 —
을 다음으로 해결한다:

- **능동적 온보딩 튜토리얼** — 정적 설명이 아니라 다음에 뭘 할지 실시간으로 안내 (예전엔 크롬 확장이 담당, 지금은 COPEN 웹 자체 온보딩으로 이전 — 3-1 참고). **진짜 해자.**
- **미션 + 스킨(게임화)** — 순서를 대신 정해주고, 하나씩 깨는 구조라 난이도·자료 과다 문제를 우회. **진짜 내기(3-1 참고)**
- **상호작용 학습** — 정보를 읽는 게 아니라 AI와 직접 주고받으며 배움. 이게 핵심 차별점

**⚠️ 올가미(팝업 AI)는 이 목록에서 뺐다** — 구글이 이미 무료로 제공하는 기능과 동일해 차별점이
아니다. 부가기능으로 강등(3-1 참고).

**3절(경쟁 구도)은 이 재확인으로 다시 유지된다** — 타겟이 리포트 3으로 안 바뀌었으므로,
"정적 가이드·유료 코스와 직접 경쟁"이라는 기존 우려는 완화된다. 다만 위 차별점이 실제로
먹히는지는 실사용자 검증 전까지는 가정일 뿐이다.

### 1-3. ⚠️ 크롬 확장의 위상 재검토 — 한 줄 목적까지 흔들림

**계기:** 웹 화면 구조(사이드바)를 설계하다가, "미션이 `More`에 숨어 있는데 첫 방문자도 똑같이
보는가"를 물었다. 답 과정에서 "웹 자체에 온보딩 튜토리얼을 만들면 되고, 그러면 **기존 크롬 확장이
필요 없을 수도 있다**"는 결론이 나왔다. 확장이 없어지면 수동적 사용자(리포트 5)의 유입은
**순수히 마케팅(광고·SNS·Product Hunt)에만 의존**하게 된다.

**이게 왜 사소하지 않은가:**

1. **한 줄 목적이 성립 근거를 잃는다.** 옛 목적("ChatGPT를 처음 켰을 때...")은 사용자가
   **기존 ChatGPT 화면을 계속 쓴다**는 전제였다. 온보딩부터 미션까지 COPEN 웹 안에서 끝나면,
   사용자는 ChatGPT 화면을 아예 안 보고 COPEN 화면만 본다. 그러면 이건 "ChatGPT 사용을
   도와주는 도구"가 아니라 "AI 활용법을 가르치는 별도 학습 제품"이다. **다른 사업이다.**
2. **2절의 리포트 5 선정 근거 일부가 약해진다.** 2절은 "리포트 5는 AI 툴 화면 안에서 마주치는
   것 말고는 닿을 채널이 없다"를 근거로 들었다. 확장이 그 유일한 채널이었는데, 확장이 없어지고
   마케팅(광고·SNS)만 남으면 — 마케팅은 애초에 아무 타겟이나 닿을 수 있으므로 **"이 타겟에게만
   닿는 특별한 채널이 있다"는 차별점 자체가 사라진다.** 타겟이 틀렸다는 뜻은 아니지만, 근거를
   다시 세워야 한다.
3. **확장에 들인 작업(오늘 끝낸 대화 기록 배관 포함)의 앞날이 불확실해진다.** 버릴지, 별도
   마케팅 채널로 남길지는 미정 — **결정된 바 없음. 성급히 폐기하지 말 것.**

**✅ 2026-08-01 결정:** "웹 자체에 온보딩 튜토리얼을 만들면 확장이 필요 없다"는 이 절의
가능성이 확정으로 바뀌었다 — COPEN 웹이 자기 온보딩 튜토리얼을 갖는다(3-1 참고). 크롬 확장의
DOM 스크래핑/화살표 코드 자체는 재사용하지 않지만, "능동적으로 다음 행동을 제시한다"는
패턴은 웹 온보딩으로 그대로 이전된다.

**한 줄 목적은 확정됐다(위 참고) — 미션은 목적이 아니라 상호작용을 이끌어내는 매개다.**

### 1-4. ✅ 크롬 확장 폐기 결정

**사유:**

- **설치 장벽이 사이트 방문보다 크다.** 스토어 방문 → 설치 → 권한 승인은, "이미 있는 화면
  안에서 마주친다"는 원래 장점을 무색하게 할 만큼 그 자체로 능동적 행동이다. 1-3에서 확인했듯
  확장이 없어도 유입은 어차피 마케팅이 담당하므로, 이 장벽을 감수할 이유가 사라졌다.
- **DOM 스크래핑 기반이라 깨지기 쉽다.** ChatGPT가 UI를 바꾸면 화살표 오버레이가 엉뚱한
  곳을 가리키게 된다. 6절·10절에 이미 "크롬 자동 표시 미보장" 리스크로 기록돼 있던 문제다.
- **"실제 ChatGPT 화면을 가리킨다"는 이점이 새 목적에서 별 의미가 없다.** 확정된 한 줄
  목적은 "AI와 상호작용하는 능력을 기르는 것"이지 "ChatGPT 버튼 위치를 외우는 것"이 아니다.

**남기는 것:** 코드는 git 기록에 남는다 — 삭제하지 않는다. 특히 `server/ask-claude.js`,
`PROMPTS.md` 1·2절, 대화 기록 배관 로직은 **UI와 무관**하므로 그대로 COPEN 웹의 백엔드로
재사용 가능하다. 새로 만들어야 하는 건 프론트(웹 UI)뿐이다. `chatgpt-tutorial-extension/`의
`content.js`(오버레이)·`chatbot.js`(팝업 UI)만 더 이상 개발하지 않는다.

**해소됨:** 2절의 "특별한 채널이 있다"는 근거는 틀렸던 것으로 정정했으나(2절 참고),
재검토 결과 **타겟(리포트 5)은 그대로 유지**하기로 확정했다.

### 미션 구성 (2026-08-01 갱신 — `CURRICULUM.md` 확정)

**✅ 위 "초기 예상 3개"는 폐기, `CURRICULUM.md`로 대체.** 전자책·유료 강의·영상 조사를 바탕으로
24개 원본 영역 → 12단계 커리큘럼 → **v1 최소 미션 18개**까지 설계 완료. 전체 내용은
`CURRICULUM.md` 참고 — 이 문서엔 요약만 남긴다.

**단계적 검증 순서 확정 (PD1 방지):** 18개를 한 번에 만들지 않는다.
`18개 설계 → 3개만 먼저 구현 → 실사용자 검증 → 통과하면 나머지 15개 추가`.
적응형 허브(선행 관계·추천 로직·능력별 판정)는 계정+DB가 필요해 4절의 "저장소는 마지막
Phase만" 확정과 충돌한다 — **v1 검증 전까지 착수 안 함**(`CURRICULUM.md` 2부 참고).

**✅ v1 3개 확정 (2026-08-01):**

| 순위 | 미션 (18개 중) | 고른 이유 |
|---|---|---|
| 1 | 4. AI의 틀린 답 잡아내기 | 6-5·databank PD2/PC1이 최고 신뢰도로 기록한 리스크(완전 초보=검증 능력 0)를 미션이 실제로 완화하는지 직접 확인 가능. 신호 가치가 가장 큼 |
| 2 | 5. 내 조건에 맞는 메뉴·일정 만들기 | 마찰 가장 낮고 "와, 되네" 체감이 가장 빠름. 기존 "밥 메뉴 추천" 아이디어와 연속성 |
| 3 | 6. 보내기 어려운 문장 다듬기 | 5번과 다른 상호작용 패턴(한 방 생성이 아니라 여러 번 고쳐나가는 대화형 개선) 검증 |

세 미션 모두 **텍스트만, 계정·DB·파일업로드 없이 배우기용 AI(이미 구현됨)로 바로 시작 가능** —
새 백엔드 작업 불필요.

**⚠️ 확인 필요한 것 — 미션 1번("AI에게 첫 질문 보내기")은 후보에서 뺐다.** 3-1에서 확정한
COPEN 웹 온보딩 튜토리얼이 "첫 질문을 보내보세요"를 이미 다룰 가능성이 높아, 별도 미션으로
두면 온보딩과 겹칠 수 있다. 온보딩 설계 때 다시 판단.

**주의: 코딩 미션(6번 카테고리, 4개)은 v1 3개 후보에 없다.** 전부 텍스트 AI 대화 미션이다.
코딩 미션은 팝업 AI(코딩 보조)와 별개로, 18개 중 나머지를 추가하는 단계에서 다룬다.

**단, 팝업 AI(코딩 보조)는 미션 단계와 무관하게 지금부터 상시 기능이다** — 웹 사이드바
최상단에 처음부터 들어간다. 역할: 올가미로 화면 일부를 선택해 질문하기 + 바이브 코더·코딩
입문자의 기초 학습 보조. `PROMPTS.md` 2절 프롬프트를 그대로 쓴다.

### 팝업 AI — 올가미 기능 명세 (2026-07-30)

**막힌 지점:** "화면 아무 곳이나(외부 사이트 포함) 자유롭게 드래그해서 선택"은 **웹사이트로는
기술적으로 불가능하다.** 브라우저가 same-origin policy로 막아둔 영역이다 — 그렇게 허용하면
아무 사이트나 사용자 화면을 몰래 캡처할 수 있게 된다. 브라우저 확장이나 네이티브 앱이 있어야만
되는 권한인데, 확장은 1-4에서 이미 폐기했다.

**해결책 — OS 내장 스니핑 도구 + 붙여넣기 (확정):**

```
사용자가 Win+Shift+S(또는 Mac Cmd+Shift+4)로 화면 아무 곳이나 자유롭게 드래그해서 캡처
  → 클립보드에 자동 복사
    → COPEN 팝업 AI 채팅창에 Ctrl+V로 붙여넣기
      → 이미지(+질문 텍스트)를 Claude에 vision 입력으로 전송
```

사용자 입장에서는 "화면에서 드래그 한 번으로 선택"이라는 경험이 그대로 유지된다 — 그 드래그를
OS의 기존 도구가 대신 처리할 뿐, COPEN이 만들 건 **채팅창의 이미지 붙여넣기 지원**뿐이다.
확장·네이티브 앱·특별 권한이 전혀 필요 없다.

**남은 구현 항목:**
- COPEN 팝업 AI 채팅 입력창에 클립보드 이미지 붙여넣기(paste) 지원
- 서버(`server/ask-claude.js`에 준하는 새 모듈)가 이미지 콘텐츠 블록을 Claude Messages API로 전송
  (Claude는 vision 지원 — `claude-api` 스킬 참고)
- 시스템 프롬프트는 `PROMPTS.md` 2절 그대로 재사용

---

## 2. 타겟 오디언스 — SparkToro 3개 리포트 비교 결과

| 리포트 | 오디언스 | 방문 사이트 | 해석 |
|---|---|---|---|
| 1 | AI 생산성 크리에이터 팔로워 | Anthropic.com, Perplexity, **n8n.io**, GitHub, Producthunt | **초보가 아님.** 이미 자동화 툴을 쓰는 파워유저/빌더층 |
| 3 | ChatGPT 배우는 초보자 | Edx, **Learnprompting.org**, Promptingguide.ai, Deeplearning.ai, Skillshare | **능동적 학습자.** 스스로 학습 사이트를 찾아감 → 정적 가이드·유료 코스와 직접 경쟁 |
| 5 | 가끔 쓰는/AI 궁금한 성인 | **Lifehacker, Kiplinger, BHG, AllTrails, Schwab, Zoom** | **AI 학습 사이트를 전혀 안 감.** 수동적 사용자 |

### 전략적 결론
**리포트 5가 오버레이 제품의 진짜 타겟일 가능성이 높다.** 이들은 정적 가이드도, "AI한테 직접 물어보라"는 요령도 스스로 발견하지 못한다.

~~즉 AI 툴 화면 안에서 직접 마주치는 것 말고는 이들에게 닿을 채널이 없다.~~

**⚠️ 위 문장은 2026-07-30에 틀린 것으로 확인됨(1-3·1-4 참고).** "확장은 화면 안에서 공짜로
마주친다"는 전제 자체가 틀렸다 — 확장도 설치(스토어 방문→설치→권한 승인)를 시키려면 처음엔
똑같이 외부 마케팅이 있어야 한다. 즉 확장이 "특별한 채널"이었던 적이 없다. 어차피 외부
마케팅이 필요하다면, 설치 마찰이 있는 확장보다 마찰 없는 웹이 비용 대비 효율이 낫다 —
그래서 1-4에서 확장을 폐기했다. **타겟(리포트 5)이 틀렸다는 뜻은 아니다** — 다만 "이 타겟에게
닿는 유일한 방법이 우리한테만 있다"는 방어 논리는 성립한 적이 없었다. **타겟은 그대로
리포트 5로 유지 확정** — 재검토 결과 바꿀 이유가 없다는 판단.

**양날의 검:** 능동적으로 배우려 하지 않는다는 건 **지불 의사도 낮을 수 있다**는 뜻. 이 그룹의 payment-signal은 미검증.

**데이터 품질 주의:** 리포트 3의 "hidden gems"에는 무관한 잡음(어덜트 사이트, GitHub 인프라 도메인)이 섞여 있었다. 상위 6개만 신호로 취급할 것.

---

## 3. 경쟁 구도

### 🚨 올가미 제스처는 이미 구글이 무료로 제공 중 (Phase 3에 직접 영향)

- **Circle to Search** (안드로이드 전역) — 화면에 동그라미 → Gemini에 질문
- **Gemini Live 화면 공유** — 실시간 화면 스트리밍 + 음성 대화
- **Chrome "Select from Screen"** (Chrome 149) — **브라우저 내장.** 화면 일부 선택 후 질문. 설치 불필요, 무료

**의미:** Phase 3(올가미)을 핵심 차별점으로 삼으면 경쟁 상대가 스타트업이 아니라 **구글**이 된다.

**아직 살아있는 차별점:** 구글의 것들은 전부 **수동적(reactive)** — 사용자가 먼저 선택하고 물어봐야 작동한다. 이 프로젝트의 Phase 1은 **능동적(proactive)** — AI가 먼저 "다음은 여기를 누르세요"라고 순서를 제시한다. **이 능동성이 구글이 아직 하지 않는 영역이고, 이 제품의 진짜 해자다.**

→ **Phase 1이 본체, Phase 2·3은 보조. 우선순위를 뒤집지 말 것.**

### 3-1. ✅ 모순 해소 — 해자는 크롬 확장이 아니라 "능동성"이었다 (2026-08-01)

**드러난 모순:** 위 경고는 "Phase 1(능동적 오버레이)이 해자, Phase 3(수동적 올가미)은 아니다"였다.
그런데 1-4에서 Phase 1의 실체였던 크롬 확장을 폐기했고, 같은 세션에서 1-2절에 올가미(팝업 AI)를
차별점으로 다시 적어 넣었다 — **정확히 이 경고와 충돌했다.** 크롬 확장이 사라진 지금 남은 건
구글이 이미 무료로 주는 것과 똑같은 수동적 올가미뿐이었다.

**해소:** 해자는 "크롬 확장"이라는 그릇이 아니라 **"능동성"이라는 성질**이었다. 그릇(확장)은
버렸지만 성질(능동성)은 옮길 수 있다 — **COPEN 웹 자체가 자기 온보딩 튜토리얼을 갖는다.**
ChatGPT 화면 버튼을 가리키는 대신, COPEN 자기 화면과 미션을 향해 "다음은 여기를 누르세요"를
제시한다. 1-3에서 "확장 없이도 웹에 온보딩을 만들 수 있다"고 열어뒀던 가능성이 이제 결정으로
확정된다.

**팝업 AI/올가미의 위상 재확정:** 부가기능이다. 있으면 좋지만(사용자가 기대하는 "AI가 화면을
같이 봐준다"는 경험), 구글이 이미 공짜로 주므로 경쟁 우위의 근거가 될 수 없다. 1-2절의
"차별점" 목록에서도 이 지위로 내린다.

**진짜 내기는 미션 시스템이다.** 사용자와의 대화에서 나온 표현을 그대로 쓰면: **"사람들이
배우는 데 직접적인 매개체가 미션"** 이다. 능동적 온보딩은 그 미션으로 사용자를 데려가는
입구일 뿐 — 미션 자체(내용·순서·상호작용 설계)가 실제로 사람을 붙잡는지가 검증해야 할
진짜 가설이다. 지금 이게 전혀 검증 안 된 상태라는 것이 12절의 핵심 리스크다.

### 온보딩 오버레이 시장 — 선례가 없다

Appcues, WalkMe, Userpilot, Chameleon, Helppier, Inline Manual 조사 완료. **전부 1st-party 구조**(제품 소유 회사가 자기 앱에 설치하는 B2B SaaS).

**"타사 앱 위에 제3자가 얹는 오버레이"는 이 카테고리에 단 하나도 없었다.** 해석이 두 갈래이고 아직 판별 불가:
1. 아무도 시도 안 한 진짜 기회
2. 시도했다가 조용히 접힘 (망한 스타트업은 SEO 흔적을 안 남김)

실패 사례를 명시적으로 검색했으나 **찾지 못함.** "경쟁자 없음"을 "기회 있음"으로 자동 해석하지 말 것.

### 지불 의사 — 존재는 확인됨
Gumroad에 "ChatGPT 초보자 가이드"류 유료 제품이 **수십 개, $1~$40대**에 판매 중.
- ✅ payment-signal 자체는 실재
- ⚠️ 그 수요를 **이미 정적 콘텐츠(PDF/전자책)가 흡수 중.** 오버레이의 우위를 증명해야 함
- ⚠️ 이 가격대(일회성 $1~40)가 향후 가격 책정의 현실적 앵커. 고가 구독 모델은 근거 약함

---

## 4. 확정된 결정 — 재논의 금지

새로운 근거 없이 뒤집지 말 것.

| 항목 | 결정 | 이유 |
|---|---|---|
| 제품 형태 | 크롬 확장 프로그램 | ChatGPT가 웹이라 배포·유지보수가 가장 쉬움 |
| 대상 툴 | ChatGPT 하나만 | 완전 초보는 Claude Code/Cursor의 존재 자체를 모름 |
| 가이드 표시 | content script로 페이지 위 화살표 오버레이 | 사이드패널은 실제 버튼을 가리킬 수 없어 1차 시도에서 실패 |
| 단계 진행 | 오버레이 자체의 확인 버튼 클릭만 감지 | ChatGPT 동작을 감시하지 않음. 자기주도 페이스 = 아는 사용자는 빨리 넘어감 |
| 가이드의 AI 사용 | 없음 (사전 작성 대본) | 원가 0, 정확도 100% |
| AI 호출 시점 | 사용자가 능동적으로 질문할 때만 | 원가 예측 가능 |
| 답변 언어 | 영어 | 타겟이 미국 오디언스 (단, 6-2의 긴장 참고) |
| 출력 형식 | ~~JSON 강제 (구조화 출력)~~ → **자유 자연어** | 2026-07-30 폐기. 아래 4-1 참고 |
| 스트리밍 | 안 씀 → 칸 단위 한 번에 + 로딩 점 | |
| confidence 필드 | 뺌 → 대신 공식 문서 링크(doc_link) | |
| 되묻기(clarification) | optional (모호할 때만) | 비용·속도 절약 |
| 저장소(오답노트) | 마지막 Phase + 로컬 저장만 | 신생 업체의 데이터 보관은 별도 반대 사유(databank PD3) |
| API 키 취급 | 로컬 개인 테스트용. 하드코딩 금지, 환경변수만, 배포 안 함 | |

### ⚠️ 폐기된 원칙
> ~~"Phase 1이 실사용자 테스트에서 긍정적 반응을 얻기 전까지 Phase 2~5로 넘어가지 않는다"~~

**이 원칙은 공식 폐기한다.** 실사용자 확보가 구조적으로 막혀(6-1 참고) 지켜질 수 없었고, 실제로 Phase 2가 이미 상당히 진행됐다. 다만 **검증 자체가 불필요해진 것은 아니다** — 언제 어떻게 할지는 여전히 열린 과제다(6-1).

### 4-1. 🔄 2026-07-30 방향 전환 — JSON 출력 폐기, 자유 자연어로

**폐기 사유** (3차에 걸친 AI 교차검증 + 사용자 판단):
- 답변이 정해진 칸을 채우는 것처럼 보였다
- 반복적이고 부자연스러웠다
- 상황에 따라 자유롭게 설명하기 어려웠다
- 구조 관리가 실제 대화 품질보다 중요해졌다

**새 방식:** 본문은 완전 자유 자연어. **버튼·enum·후미 태그·행동 채널·tool-calling 전부 없음.**
UI(팝업 닫기, 코드 복사, 미션 복귀 등)는 AI 출력과 무관하게 프론트엔드가 소유한다.
프롬프트는 `PROMPTS.md` 참고.

**검토 과정에서 기각한 중간안들 — 재논의 금지:**

| 안 | 기각 사유 |
|---|---|
| 후미 마크다운 태그 (`[BTN: ...]`) | 정규식 한 줄로 안 됨. 태그 누락·중복·코드블록 내 오인·사용자 입력 충돌 등 파서/허용목록/이스케이프 문제 전부 재발 |
| XML 블록 분리 | JSON을 XML로 바꾼 것에 불과. 경직성 그대로 |
| `choose_actions` tool-calling + 행동 enum | 채널 분리는 맞았으나, 행동 목록을 유지·주입·검증하는 부담이 미션 수에 비례해 증가 |
| `[CODE_APPLY: HTML_LINE_12]` 류 자동 적용 | 줄 번호 기반은 취약. diff·승인·롤백 인프라 없이는 도입 불가 |
| 미션 **진행도**를 AI에 전달 | 매 요청 상태 전달 + 미션마다 완료 정의 필요. 미션이 늘수록 관리 부담 증가 |

**단, 미션 "내용"(커리큘럼 구성)과 가이드 목차 주입은 기각하지 않았다** — 고정 목록 하나라
비용이 다르다. `IDEAS.md` 4·5번 참고. 둘 다 해당 문서가 아직 없어서 보류 중.

**AI는 미션 진행도를 모른다.** 따라서 완료 판정은 전적으로 기존 가이드/튜토리얼 시스템이 담당하며,
프롬프트에도 "진행 상황을 추측하지 말라"가 명시돼 있다.

**보관하되 출력 형식으로는 안 쓰는 것:** `chatbot/schema.v1.json`, `semantic_validator.js`,
`validate.js`, `claude-runner-task2/` 일체. 삭제하지 않는다 — 나중에 응답 품질을 사후 점검할 때
재활용 가능하고, 7절의 실측 데이터(문법 크기 한계선 등)는 그 자체로 기록 가치가 있다.

**⚠️ 이 전환으로 7절의 상당 부분이 역사 기록이 됐다.** 7절의 스키마 8종·파이프라인·24회 라이브
테스트 결과는 *"그때 그렇게 했고 이런 걸 알아냈다"*는 기록이지 현재 설계가 아니다.
`chatbot.js`의 렌더러도 JSON 스키마 기반이라 자연어 출력에 맞게 다시 손봐야 한다(미착수).

---

## 5. 이미 검토하고 기각/보류한 것 — 재논의 방지

| 항목 | 판정 | 이유 |
|---|---|---|
| 사이드패널 방식 | 기각 | 페이지와 분리된 영역이라 실제 버튼을 못 가리킴 (1차 시도 실패) |
| 네이티브 데스크톱 앱 (화면 캡처 + AI 비전) | 기각 | ToS는 더 안전하나 화면 녹화 권한 요청이 비기술 사용자에게 더 큰 장벽. 실시간 비전 API 비용도 과다 |
| 터미널 텍스트 감지로 단계 완료 확인 | 기각 | 오버레이 자체 버튼 클릭 감지로 충분. 훨씬 단순 |
| Claude Code / Cursor를 첫 타겟 툴로 | 기각 | 완전 초보는 그 툴의 존재 자체를 모름 |
| 팝업 "직접 질문"을 핵심 차별점으로 | 축소 | Chrome 내장 기능이 이미 무료 제공. 보조 기능으로만 |
| AI 파인튜닝으로 "전문화" | 불필요 | 시스템 프롬프트만으로 톤·형식·페르소나 전부 통제 가능 |
| 4개 모델 × 14개 설계질문 × 3회 반복 대규모 벤치마크 | **보류(기각 아님)** | 지금 하기엔 과함(수백 회). 제품이 실제로 쓰인다는 확인 후 실행. 문서는 보관 |

---

## 6. 리스크

### 6-1. 🚨 실사용자 검증이 구조적으로 막혀 있음
게으름이 아니라 제약이다: **사용자 주변에 테스트 가능한 사람이 1명뿐.**

**진짜 과제는 "5~10명을 어떻게 구하는가"다.** 후보:
- Discord: Learn AI Together(약 10만명), r/ChatGPT Discord(약 25만명), ChatGPT Community(약 6.6천명)
- ⚠️ **OpenAI 공식 Discord는 피할 것** — 경쟁 제품으로 보여 즉시 삭제/밴 위험
- Reddit은 **계정 정지로 사용 불가**
- 한국 커뮤니티(네이버 카페, 카카오 오픈채팅) — 단, 6-2의 언어 문제

**Discord 게시 여부 불명:** 대화 중 "올렸다"와 "지체돼서 넘어갔다"가 모두 나왔다. **완료로 단정하지 말고 실제 상태를 먼저 확인할 것.**

### 6-2. 타겟 언어 vs 테스트 가능한 사람 (미해결)
답변 언어는 영어, 타겟은 미국(SparkToro 전부 US 기준). 그러나 사용자는 한국에 있고 접근 가능한 테스트 대상자는 한국어 사용자다.

한국인에게 영어 UI 제품을 테스트시키면 **"제품이 어려운 건지 영어가 어려운 건지" 구분이 안 된다.** 결과 해석이 오염될 수 있음.

### 6-3. ToS는 회색지대 (배포 전 재검토 필수)
- OpenAI 이용약관은 *"Modifying... any of our Services"* 를 **명시적으로 금지**
- Superpower ChatGPT 등 기존 확장이 살아있으나, 이는 **"허용됨"이 아니라 "아직 단속당하지 않음"의 증거일 뿐**
- 판례나 공식 입장 표명 없음

좌표만 읽고 별도 레이어를 얹는 설계가 리스크를 **줄이는 것은 맞지만 제거하지는 못한다.** 사업 존립이 상대 회사의 묵인에 달려있다는 구조적 리스크는 그대로.

### 6-4. 확장 프로그램의 온보딩 역설
개발자 커뮤니티 증언: **AI 크롬 확장의 최대 이탈 지점은 "설치 이후 온보딩" 그 자체.**

타겟이 "설치·설정을 어려워하는 비기술 사용자"인데, 그들을 돕는 도구가 **또 하나의 설치·설정 장벽**이 된다. "ChatGPT 쓰는 법을 몰라 헤매는 사람"이 "크롬 웹스토어에서 확장을 찾아 설치하고 권한을 허용"할 수 있는가 — 미검증.

### 6-5. 챗봇 할루시네이션 리스크가 일반 제품보다 크다
타겟이 완전 초보 = **검증 능력 0.** 일반 사용자는 "어? 이상한데?" 하고 검색하지만, 이 타겟은 틀린 답을 그대로 믿는다. databank PD2/PC1이 최고 신뢰도로 기록한 패턴이다.

대응: doc_link 자동 첨부, 자주 묻는 질문은 **사람이 미리 검수한 답**으로 캐싱.

---

## 7. 현재 상태

### 폴더 구조
```
pppp/
├─ PROJECT.md                     ← 이 문서 (유일한 기준 문서)
├─ AI_ROLE_PROMPT.md              ← 후속 AI 역할 지시서
│
├─ chatgpt-tutorial-extension/    ← 실제 크롬 확장 (git, GitHub push됨)
│   ├─ manifest.json              (v2.1, host_permissions에 중계 서버 주소 등록)
│   ├─ content.js                 가이드 오버레이 (Phase 1, 약 800줄)
│   ├─ chatbot.js                 챗봇 팝업 (Phase 2, AI 연결 완료 — sendToAI가 background 경유로 중계 서버 호출)
│   └─ background.js              아이콘 클릭 → 가이드 재시작 / "ask-ai" 메시지를 중계 서버로 fetch
│
├─ chatbot/                       ← 챗봇 응답 스키마 설계/검증 (별도 AI 제작)
│   ├─ schema.v1.json             원본 스키마 (판정 기준 = 진짜 계약)
│   ├─ schema.v1.openai-strict.json  생성 요청용 변환본 (OpenAI용, Claude엔 안 씀)
│   ├─ examples.json              통과 예시 8종
│   ├─ negative.js                반례 20종
│   ├─ validate.js                ajv 검증 스크립트
│   └─ semantic_validator.js      의미 검증기
│
├─ claude-runner-task2/           ← Claude 실제 API 생성 테스트 러너 (별도 AI 제작 + 수정)
│   ├─ claude-adapter.js          Claude Messages API 어댑터 (강제 출력 + 프롬프트 모드 둘 다 지원)
│   ├─ generate-validated.js      분류→생성→검증→재생성 파이프라인 (server/가 이걸 그대로 가져다 씀)
│   ├─ run-eval.js                8종×3회=24회 생성 테스트 + 리포트 생성
│   ├─ test-single-type.js        진단용: body 1종만 남긴 축소 스키마로 1회 테스트
│   └─ offline-check.js           오프라인 검증 (ajv + semantic_validator, API 호출 없음)
│
└─ server/                        ← 확장 프로그램 ↔ Claude API 중계 서버 (API 키는 여기에만 존재)
    ├─ index.js                   POST /ask → generate-validated.js 호출 → 결과 반환
    └─ package.json               `npm start` (기본 포트 8787)
```

### Phase 0 — 개발 환경 ✅ 완료
Node.js, Claude Code CLI 설치. Claude 구독 계정 로그인(계정 혼동 해결됨). GitHub 계정·저장소 생성. 로컬 → GitHub → claude.ai/code 전환 완료.

### Phase 1 — 가이드 오버레이 ✅ 대강 완성
`content.js` (약 800줄), GitHub push 완료.
- 3개 섹션: **Basics / Explore / Settings & privacy**
- 실제 페이지 버튼을 화살표로 가리킴 (Shadow DOM 오버레이, 좌표 추적)
- 완료 화면, × 건너뛰기, 첫 방문 자동 표시 + 아이콘 재실행
- 문구 전부 영어, AI 호출 없음
- 상호작용 버그 해결 완료. `/simplify` 정리 1회 (커밋 `0dc70a3`)

### Phase 2 — 전문화 챗봇 🔄 진행 중

**만든 것: 팝업 껍데기 + 표시 구조 (`chatbot.js`, 커밋 `992e83a`)**
- 오른쪽 아래 떠있는 별도 팝업 (가이드와 분리된 Shadow DOM), 드래그 이동, 최소화/재열기
- 답변을 소제목 칸 단위로 표시 + 로딩 점
- **AI 미연결**: `sendToAI()`가 stub JSON 반환. 이 함수만 실제 API로 교체하면 됨

**응답 스키마 (`chatbot/schema.v1.json`)** — `body.type`이 유일한 판별자, 8종:
`concept_explanation / prompt_help / procedure / code_explanation / code_generation / error_diagnosis / clarification_request / safety_notice`

핵심 설계:
- **reliability / safety** 상태를 매 응답 선언
- **audience.level + tier(core/more)** 로 초보에게 한 번에 노출할 양 제한
- **origin** (`user_provided` vs `assistant_authored`) — 사용자 코드를 지시가 아니라 데이터로 취급 = 프롬프트 인젝션 방어
- 검증: ajv로 예시 8/8 통과, 반례 20/20 차단

**의미 검증기 (`semantic_validator.js`)** — Schema가 구조로 못 잡는 위반을 후처리. `check(response, context) -> string[]`. 6규칙, 유닛테스트 12/12 통과, 기존 예시 오탐 0:
1. safety_notice인데 safety=standard
2. 사용자 코드인데 origin이 user_provided 아님
3. 실제 오류인데 representative(지어낸 오류)로 처리
4. 초보인데 core tier 블록 4개 초과
5. 파괴적 명령(rm -rf / sudo)인데 safety=standard
6. reliability=stable인데 시제 표현("최신"/"현재 버전") 있음

**API 제공사: Claude로 확정** (`claude-sonnet-5`, `output_config.format` 구조화 출력 사용).

**🚨 실제 생성 테스트에서 발견한 핵심 사실 (Claude용 스키마 재설계 필요):**
- `schema.v1.json`을 8종(`body.type`) 통째로 한 번에 Claude에 보내면 **거부됨**: `"The compiled grammar is too large, which would cause performance issues."`
- `maxLength`/`minItems`/`maxItems` 같은 Claude 미지원 키워드를 제거해도 이 문제는 그대로 (별개 원인)
- **8종 중 1종만 남기고, 안 쓰는 `$defs`까지 다 잘라낸(33개→11개) 축소 스키마로는 성공**
- 그러나 **축소만으로는 작은 타입만 통과한다.** 라이브 실측:

| 시도 | 크기 | 모양 가짓수 | 펼친 속성 | 결과 |
|---|---|---|---|---|
| 통짜 원본 (8종 전부) | 14,291자 | — | — | ❌ 거부 |
| 축소: concept_explanation | 4,493자 | 9 | 76 | ✅ **통과** |
| 축소: code_generation | 7,199자 | 140 | 223 | ❌ 거부 |
| 축소+평탄화: code_generation | 5,738자 | **1** | — | ❌ 거부 |

- **기각된 가설: "선택지(anyOf) 조합 폭발이 원인"** — 평탄화로 모양 가짓수를 140→1로 만들었는데도 거부됐다. 조합 수는 무관하다. (평탄화 도구는 `chatbot/flatten-unions.js`에 남겨뒀으나 효과 없음이 실측으로 확인됨 — 재시도하지 말 것)
- **기각된 가설: "description이 크기를 차지한다"** — 전부 제거해도 8%만 줄어든다.
- **기각된 가설: "선택 필드가 많아서 Claude의 선택 매개변수 24개 제한에 걸린다"** — 8종 전부 선택 필드가 **0개**다(모든 속성이 `required`). 무관하다.
- **한계선은 단일 수치로 환원되지 않는다.** Claude 내부 문법(grammar) 크기는 속성 수·중첩 깊이·`$ref` 반복·유니온이 함께 만들어낸다. 다만 작은 4종과 큰 4종은 아래 지표 전부에서 깨끗하게 갈린다:

**8종 전부 라이브 실측 완료:**

| body.type | 크기 | 펼친 속성 | 객체 수 | 유니온 | 최대 깊이 | API | AJV |
|---|---|---|---|---|---|---|---|
| safety_notice | 4,273자 | 68 | 23 | 2 | 9 | ✅ | ✅ |
| concept_explanation | 4,493자 | 76 | 26 | 2 | 9 | ✅ | ✅ |
| prompt_help | 4,335자 | 77 | 26 | 2 | 9 | ✅ | ✅ (원인 규명·수정 완료) |
| clarification_request | 4,492자 | 82 | 26 | 2 | 9 | ✅ | ✅ |
| code_explanation | 6,508자 | 145 | 43 | 5 | 9 | ❌ 거부 | — |
| procedure | 6,135자 | 186 | 49 | 10 | 16 | ❌ 거부 | — |
| error_diagnosis | 6,134자 | 195 | 52 | 11 | 12 | ❌ 거부 | — |
| code_generation | 7,199자 | 223 | 57 | 10 | 16 | ❌ 거부 | — |

- **한계선은 4,492자(통과)~6,134자(거부) 사이.** 작은 4종만 강제 구조화 출력이 가능하고, 큰 4종은 불가능하다.
- `code_explanation`은 모양 가짓수가 18뿐인데도 거부됐다 → **"모양 가짓수(anyOf 조합)가 원인"이라는 기각된 가설이 한 번 더 확인됨.**
- `code_generation`에서 `file_source_code`가 7회, `source_code`가 5회 재사용된다. **`$ref` 자체가 아니라 큰 구조를 여러 위치에서 반복 참조하는 것**이 문법을 부풀린다.
- **별개 문제, 해결됨:** `prompt_help`가 API는 통과했으나 원본 스키마 AJV 판정에 실패한 적이 있다(`principles`에 5개를 넣었는데 원본 제한은 4개). 원인은 생성용 스키마에서 미지원 키워드를 지우기만 하고 그 제약을 모델에게 알려주지 않은 것. 지우는 대신 `description`에 "(최대 4개)"처럼 문장으로 옮겨 전달하도록 고쳤다.
- 관련: databank **PD1(검증 비용의 역설)** — 이 스키마는 실제 모델에 한 번도 붙여보지 않은 채 정교하게 설계됐다. 지금 그 청구서가 온 것.

**✅ 파이프라인 구현 완료 (`generate-validated.js`) — 라이브 8/8 통과:**

```
질문 → ① 분류(작은 스키마, 강제 출력) → ② 해당 타입 스키마로 생성
     → ③ 원본 schema.v1.json AJV + semantic_validator(block) 판정
     → 실패 시 위반 목록을 돌려주고 1회만 재생성
```

- ②는 **강제 출력을 먼저 시도하고 거부당하면 프롬프트 방식으로 내려간다.** "큰 4종"을 코드에 이름이나 크기 숫자로 박지 않은 이유: 한계선(4,492~6,134자)은 claude-sonnet-5의 현재 사정이라 모델이 바뀌면 조용히 틀린다. 거부는 생성 전에 나므로 토큰 비용 0이고, 타입별 결과는 캐시한다.
- 프롬프트 방식에서는 **원본 스키마를 그대로 보낸다.** 문법으로 컴파일되지 않으므로 미지원 키워드를 뺄 이유가 없고, `maxItems: 4`를 그대로 보여주는 게 "(최대 4개)" 설명문보다 정확하다.
- 라이브 결과: 8종 전부 분류 일치, 전부 1회 시도로 판정 통과(재생성 발동 없음). 작은 4종은 강제 출력, 큰 4종은 프롬프트 방식으로 갈렸다.
- 이 과정에서 버그 1건을 라이브에서 발견: `extractJson`이 코드펜스 벗기기를 먼저 해서, JSON **값 안에** 든 펜스(clarification_request의 붙여넣기 틀)를 껍데기로 착각해 멀쩡한 응답을 잘라냈다. 통째로 파싱을 먼저 시도하도록 고치고 회귀 테스트를 넣었다(테스트 13/13).

**교차검증(ChatGPT) 결론 — 채택한 방향:**
1. 지금 당장은 **강제 구조화 출력을 쓰지 않는다.** 프롬프트로 JSON 형식을 요청 → `JSON.parse` → AJV(원본 `schema.v1.json`) → `semantic_validator` → 실패하면 오류 내용을 넣어 **1회만** 재생성 → 그래도 실패하면 안전한 오류 화면.
2. 실사용 가치가 확인된 뒤에 **분류 호출 → 타입별 축소 strict 스키마** 2단계로 전환한다.
3. 명백한 중복(`error_diagnosis`의 `failing_code`와 `minimal_fix.before`)은 지금 줄이되, 교육용 필드(`comprehension_check`, `analogy` 등)를 뺄지는 **실사용자 테스트 후에** 정한다.
- 이유: 실사용자가 아직 0명이라, 스키마를 더 다듬는 건 PD1이 경고한 과설계 반복이다.

**✅ Task 2/3 완료 — 24회 정식 라이브 테스트 (8종 × 3회, `npm run eval`):**

| 지표 | 결과 |
|---|---|
| 유형 분류 정확도 | 24/24 (100%) |
| 최종 통과율(재생성 포함) | 24/24 (100%) |
| **1차 시도 통과율(재생성 없이)** | 24/24 (100%) |
| semantic_validator block 위반 | 0건 (warn만 3건, 차단 아님) |
| 평균 응답시간 | 17.4초 (분류+생성 합계) |

방식별로 나눠 보면:

| 방식 | 대상 | 응답 | 1차 통과 | 평균 응답시간 |
|---|---|---|---|---|
| 구조화 출력 강제 | 작은 4종 | 12 | 100% | 21.1초 |
| 프롬프트 + 검증 | 큰 4종 | 12 | **100%** | 13.7초 |

**결론: 강제로 형식을 잡아주지 않고 "부탁"만 해도, 큰 4종 전부 12번 중 12번 한 번에 정확한 JSON을 냈다.** 오히려 강제 출력보다 빠르다(분류 단계 스키마가 작아서로 추정). 이는 "지금은 스키마를 축소하지 않고, 실사용자 테스트 후에 결정한다"는 방향을 뒷받침하는 실측 근거다. 표본이 24회뿐이라 확률 자체를 단정할 순 없지만, 지금 스키마를 더 깎을 급한 이유가 없다는 판단에는 충분하다.

**✅ 챗봇 본체 연결 완료 — Phase 2 기능적으로 완성:**

```
chatbot.js (브라우저, 확장 프로그램)
   → chrome.runtime.sendMessage({type:"ask-ai", prompt})
background.js (확장 프로그램 자체 컨텍스트, 페이지 CSP 영향 안 받음)
   → fetch http://localhost:8787/ask
server/index.js (개발자가 운영하는 서버, ANTHROPIC_API_KEY는 여기만)
   → generate-validated.js (분류→생성→검증→재생성)
   → Claude API
```

- **왜 서버를 하나 더 두었나:** 확장 프로그램 코드는 설치한 모든 사용자의 브라우저에서 실행된다. 거기에 API 키를 넣으면 설치자 전원의 브라우저에 키가 노출되고, 개발자 앞으로 무제한 비용이 청구될 수 있다. 그래서 키는 개발자가 운영하는 이 중계 서버에만 두고, 확장 프로그램은 그 서버의 `/ask`만 호출한다. (사용자가 직접 선택: "내 서버를 두고 중계" 방식 채택)
- **왜 background.js를 거치나:** `chatbot.js`는 `content_scripts`로 실제 ChatGPT 페이지 안에서 실행되므로 그 페이지의 CSP(콘텐츠 보안 정책)를 적용받는다. `background.js`(service worker)는 페이지와 분리된 확장 프로그램 자체 컨텍스트라 이 제약이 없다.
- `chatbot.js`의 답변 렌더러(`addAiAnswer`)를 전면 재작성했다. 예전 스텁은 `{topic, term_meaning, code_explanation, overall_structure, doc_link}`라는, 실제 스키마와 무관한 가짜 필드를 그렸다. 실제 `schema.v1.json`은 `body.type`마다 필드가 다르므로(4~9개, 최대 3단계 중첩), 타입별 화면을 따로 만드는 대신 필드 이름→한국어 라벨 매핑 + 재귀 렌더러로 8종을 전부 하나의 함수로 그린다. `safety.status`가 `standard`가 아니면 경고 배너, `reliability.status`가 `stable`이 아니면 안내 문구를 붙인다.
- 지금은 `--host_permissions`와 중계 서버 주소가 `http://localhost:8787` 로 고정돼 있다. **실제 사용자에게 배포하려면** 이 서버를 어딘가에 올리고(Render/Railway 등) 주소를 바꿔야 한다 — 지금은 로컬 개발/테스트 단계이므로 의도적으로 미룬 것.
- 아직 라이브로 브라우저에서 직접 확인은 안 했다. 다음 단계: 크롬에 확장 프로그램을 로드하고, `cd server && npm start`로 중계 서버를 띄운 뒤, ChatGPT 페이지에서 실제로 질문해보는 것.

**분석 도구 (전부 오프라인, 비용 0):**
`chatbot/schema-split.js`(타입별 축소) · `check-split.js`(8종 검증) · `analyze-grammar.js`(모양 가짓수) · `measure-size.js`(크기·속성 측정) · `flatten-unions.js`(평탄화, 효과 없음 확인됨)

---

## 8. 남은 로드맵

> ⚠️ **아래 Phase 3~5는 전부 "크롬 확장이 본체"라는 옛 전제로 짠 것이다**(1-1 참고).
> COPEN 웹이 본체가 된 지금, 이 로드맵은 우선순위를 다시 매겨야 한다. **미착수.**
> COPEN 웹 자체는 아직 이 표에 들어와 있지도 않다.

| Phase | 내용 | 상태 |
|---|---|---|
| 3 | **올가미 선택 질문** — 화면 일부를 드래그해 AI에 질문. Phase 2 챗봇의 "입력 방식" 중 하나로 통합(별도 기능 아님) | ✅ 구현 완료(`210e3e5`). **부가기능으로 확정, 차별점 아님**(3-1 참고) |
| 신규 | **COPEN 웹 온보딩 튜토리얼** — 능동적 안내를 웹 자체로 이전. **진짜 해자(3-1 참고)** | 미착수. 미션 시스템과 함께 최우선 |
| 4 | **팝업 UI 디자인** — 원형 → 호버 시 직사각형, 스킨/색상. 기능 완성 후 | 예정 |
| 5 | **AI 툴 사전** — 여러 AI 툴 카테고리/이름 검색 비교 디렉토리. 별도 트랙 | 예정. **강한 경쟁자(Theresanaiforthat.com) 존재 — 착수 전 재점검 필수** |

---

## 9. 지금 막혀 있는 것 / 미결정

| 항목 | 상태 |
|---|---|
| **API 제공사** | ✅ **Claude로 확정** (`claude-sonnet-5`) |
| **실제 API 생성 테스트 (Task 2·3)** | ✅ 완료(24/24 통과, 커밋 `b70bfa0`). **단 이건 JSON 파이프라인 기준 결과라 4-1 전환 이후로는 역사 기록이다.** 자연어 프롬프트는 아직 한 번도 안 돌렸다 |
| **JSON 구조 확정** | ✅ **해소됨 — JSON 자체를 폐기했다**(4-1 참고). 대신 `PROMPTS.md`의 자유 자연어 프롬프트가 그 자리를 대신한다. 단 프롬프트는 아직 **초안이고 라이브 시험 전** |
| **자연어 프롬프트 시험** | 🔧 도구는 완성(`prompt-eval/`), **아직 안 돌림.** `node prompt-eval/run.js`. 16건 중 한 턴으로 채점 가능한 것만 들어 있고, 12번(선택지 응답)은 배관 대기. 자동 채점은 안 한다 — 객관 신호만 세고 판정은 사람이 |
| **대화 기록 전달** | ✅ **완료, 라이브 확인됨(크롬 확장 기준).** 확장은 1-4에서 폐기됐지만, 로직 자체(`server/ask-claude.js`, 배열 검증, 최근 10개 자르기)는 UI 무관 — 웹의 배우기용 AI가 그대로 재사용한다. 3턴 대화로 "Alice"를 기억하는지 라이브 테스트 통과 |
| **두 AI 중 어느 프롬프트를 쓸지 앱이 판단하는 로직** | ✅ **해소됨.** 배우기용 AI와 팝업 AI(코딩 보조)가 웹 사이드바 최상단에 각자 독립된 진입점으로 존재 — 진입점 자체가 구분이라 판단 로직이 따로 필요 없다. 팝업 AI는 미션 단계와 무관하게 상시 기능(1-1 참고). `PROMPTS.md` 1·2절 프롬프트 둘 다 그대로 쓸 자리가 생겼다 |
| **COPEN 웹 (제품 본체)** | 🔧 **착수함(2026-08-01, 사용자 피드백으로 2026-08-02 수정).** `web/app.html` — 사이드바 + 채팅(빈 대화는 ChatGPT식 가운데 입력창 랜딩) + 미션(추천 1개+전체보기, 체크박스 자기 확인 게이트) + Popup AI(안내 화면→버튼→진짜 별도 브라우저 창). 라이브로 배우기용 AI 대화·미션 완료까지 확인됨. `window.open` 팝업만 이 세션의 자동화 브라우저 도구가 차단해 실제 크롬에서 재확인 필요 |
| **`chatbot.js` 렌더러** | 📦 **역사 기록.** 확장이 1-4에서 폐기됐으니 이 렌더러도 더 안 씀. **웹의 채팅 렌더러를 새로 만들어야 한다** — 마크다운 + `<details>` 접기 지원 필수(`PROMPTS.md`가 이미 이걸 전제로 쓰여 있음) |
| **실사용자 테스트** | 미실행. 6-1 참고 |
| **Discord 게시 여부** | 불명. 실제 상태 확인 필요 |

### 의견 (전임자)
API 제공사와 스키마 확정은 **실사용자 테스트 없이는 답이 안 나온다.** 아무도 안 써본 기능의 응답 형식을 완벽히 정하는 건 불가능하다. 순서를 뒤집는 것을 권한다.

또한 현재 스키마 작업(8종 판별 + 반례 20종 + 의미검증기 6규칙 + 유닛테스트 12종)은 **실제 AI를 붙여보기도 전에** 진행됐다. databank **PD1(검증 비용의 역설)** 이 정확히 이 함정이다. 추가 정교화 요청이 나오면 먼저 "실제 사람이 써봤는가"를 물을 것.

---

## 10. 지켜야 할 제약

- **API 키**: 코드에 하드코딩 금지, 환경변수(`OPENAI_API_KEY`/`ANTHROPIC_API_KEY`)로만. 채팅에 붙여넣지 말 것
- **GitHub 토큰**: 채팅 노출 시 항상 가림(`ghp_...` → `[REDACTED]`), git remote config에 저장 금지(push URL에만 임베드)
- **판정 기준**: 챗봇 응답 검증은 항상 원본 `schema.v1.json`으로. strict 변환본은 생성용일 뿐
  (4-1 이후 적용 범위 축소 — 출력 형식이 자연어라 이 규칙은 보관된 검증기를 다시 쓸 때만 해당)
- **코드 출력**: 바뀐 부분만 발췌 (초보 + 2-AI 검토 워크플로)
- **크롬 자동 표시 미보장**: 보안 정책상 오버레이가 항상 자동으로 안 뜰 수 있음. 아이콘 클릭 fallback 유지

---

## 11. databank 연결

프로젝트 파일 `ai_pain_points_databank.md`는 별도 프로젝트(세금/금융 검증) 리서치지만, 아래는 이 프로젝트에 직접 관련된다:

| 코드 | 내용 | 이 프로젝트에서의 의미 |
|---|---|---|
| **PA1** (High) | bounce loop — 초보가 가이드 사이를 계속 옮겨다님 | 근본 문제 정의. 가장 강한 근거 |
| **PA2** (Medium) | 일부는 정적 가이드 대신 AI에게 직접 물어보며 해결 | **가장 큰 경쟁자.** 테스트에서 "그냥 GPT한테 묻는 게 낫다"가 나오면 이 패턴이 이긴 것 |
| **PA3 / EA7** (High) | 제품명 혼동(Claude vs Claude Code vs Cursor)만으로 막힘, 돈 쓰고서야 알아챔 | ChatGPT를 타겟으로 한 이유. Phase 5의 근거이기도 함 |
| **PD1** (최고 신뢰도) | 검증 비용의 역설 — 검증이 실행보다 비싸지는 지점 | **현재 스키마 과잉설계가 정확히 이 함정** |
| **PD2 / PC1** (High) | 확신에 찬 부분적 오답이 명백한 오답보다 위험 | 타겟이 완전 초보 = 검증 능력 0. 6-5 참고 |
| **PD3** (Medium) | 기관·벤더 신뢰는 AI 정확도와 **별개 축** | 저장소를 로컬 전용으로 미룬 이유 |
| **PC4 / OD1** (databank 최다 검증) | "답을 주는 것"보다 **"뭘 물어봐야 할지 알려주는 것"** 이 검증된 가치 | 챗봇 "자주 묻는 질문" 버튼의 근거 |

---

## 12. 다음 세션 인수인계 (2026-07-30, 2026-08-01 갱신)

**이 세션에서 방향이 크게 바뀌었다.** 새로 시작하는 사람은 1절 전체(특히 1-1~1-4)와
이 절을 먼저 읽을 것.

### ✅ 해소됨 — 올가미/구글 경쟁 모순 (2026-08-01, 3-1 참고)

**결론만 요약(전체 근거는 3-1):** 해자는 "크롬 확장"이 아니라 "능동성"이었다. 확장은 버렸지만
능동성은 COPEN 웹 자체 온보딩 튜토리얼로 옮긴다. 팝업 AI/올가미는 부가기능으로 확정 —
구글이 이미 공짜로 제공하므로 차별점이 아니다. **진짜 내기는 미션 시스템**이다: 사람들이
배우는 실제 매개체가 미션이고, 이게 실제로 사람을 붙잡는지가 검증해야 할 진짜 가설이다.

### 오늘 결정된 것 (재논의 금지, 근거는 1-1~1-4)

- 본체는 COPEN 웹(미션), 크롬 확장은 폐기(코드는 git에 보관)
- 한 줄 목적: "AI와 직접 상호작용하며 활용법을 익히게 하는 것" — 미션은 목적이 아니라 매개
- 타겟은 리포트 5(수동적 사용자) 유지, 단 "특별한 채널"이라는 근거는 틀렸던 것으로 정정
- 팝업 AI = 올가미(OS 스니핑 도구+붙여넣기) + 코딩 보조. 미션 단계와 무관하게 상시 기능
- 서버·백엔드(`server/ask-claude.js`, 대화 기록 로직)는 UI 무관 — 그대로 재사용 중

### ✅ 미션 시스템 설계 + 웹 뼈대 착수 (2026-08-01)

**미션 판정 원칙 확정:** 4-1에서 "AI는 미션 진행도를 모른다"를 이미 정했는데, 오늘 이걸 실제
메커니즘으로 구체화했다. AI도 정규식도 "결과가 잘 나왔는지" 같은 의미 판단을 하지 않는다.
완료 버튼은 **대화 턴 수(`minTurns`)** 처럼 구조적으로 셀 수 있는 조건 하나로만 열리고, 나머지
체크리스트는 안내문이며 최종 판단은 **사용자가 직접 완료 버튼을 누르는 것**이다. 미션 설정은
`{id, title, checklist, minTurns}` 데이터로만 늘어나고 판정 코드는 공유된다 — 100개로 늘어도
같은 구조.

**입구는 1개만(명시적 미션 선택), 유형은 목표 미션만.** 자유 대화 중 우연히 조건을 채우면
제안하는 입구, 퀴즈/코드테스트용 전용 화면(도전 미션)은 v1 3개에 필요 없어 보류했다 — v1은
전부 목표 미션(사용자가 미션을 고르고, 기존 배우기용 AI 채팅에서 그대로 진행)이기 때문.

**`web/app.html` 구현·라이브 확인 완료:** 사이드바(`web/mockup-sidebar.html` 참고, 기능 연결) +
채팅(`web/popup.html` 패턴 재사용, persona `learning`) + 미션 배너(진행 중 체크리스트 + 완료
버튼, 턴 수 게이트) + 미션 목록 화면. 빌드 없는 단일 HTML, 기존 `web/popup.html`과 같은
방식 — 프레임워크 결정은 여전히 미룸(아래 항목 2). 미션 상태는 대화 기록과 동일하게 세션
메모리에만 유지(새로고침 시 초기화) — 4절 "저장소는 마지막 Phase만"과 안 부딪힘.
구현 중 실제 버그 1건 발견·수정: `startMission()`이 `activeMission`을 지정한 직후
`startNewChat()`을 불러 그 값을 도로 `null`로 지워버려 배너가 항상 안 뜨던 문제 — 순서를
바꿔 해결.

**아직 이 파일에 없는 것:** 마크다운/`<details>` 렌더링(아래 항목 5, 그대로 미해결), 프레임워크/
라우팅/배포 결정, 온보딩 튜토리얼(아래 항목 1).

### 아직 안 된 것 (우선순위 순서 아님, 그냥 목록)

1. **COPEN 웹 온보딩 튜토리얼 설계** — 3-1에서 진짜 해자로 확정됐으나 설계는 미착수. 능동적으로
   "다음은 여기"를 제시하는 패턴을 웹 자체 UI에 어떻게 구현할지 미정
2. 웹 앱의 프레임워크·라우팅·배포 방식은 여전히 미정. 지금은 `web/app.html`·`web/popup.html`
   둘 다 빌드 없는 정적 파일 — 페이지가 늘어나면 이 방식의 한계를 다시 볼 필요
3. ~~3개 미션(밥 메뉴/요약/할루시네이션) 각각 구체적으로 뭘 주고받는지 미정~~ — ✅ 해소.
   `CURRICULUM.md`로 대체, v1 3개(틀린 답 잡기/메뉴·일정/문장 다듬기) 확정, `web/app.html`에
   완료 조건까지 구현·라이브 확인 완료
4. ~~미션 진행 상황을 저장하려면 계정·DB가 필요한데, 저장소 확정 사항과 충돌한다~~ — ✅ 해소.
   v1은 의미 판정을 안 하므로(체크리스트 자기 확인 + 사용자 승인) 계정·DB가 필요 없다. 세션
   메모리만으로 충분 — 4절과 안 부딪힘. (2부의 적응형 허브로 갈 때는 이 문제가 다시 나온다)
5. `web/popup.html`·`web/app.html` 둘 다 답변 렌더러가 `textContent` 그대로다 — `PROMPTS.md`의
   `Tip:`, `<details>` 접기가 화면에 글자 그대로 보인다. 마크다운/HTML 렌더링 필요
6. `prompt-eval`의 12번(선택지 응답) 자동 시험은 여전히 미착수
7. 사이드바의 "AI 도구 찾기"는 예전 로드맵 Phase 5 — Theresanaiforthat.com이라는 강한 경쟁자가
   이미 있다고 8절에 기록돼 있음. 착수 전 재점검 필요
8. "상점"(포인트로 스킨 구매 등)은 이름만 나왔고 뭘 파는지, 포인트를 어떻게 버는지 전혀
   설계 안 됨
9. ~~배우기용 AI도 웹에 실제로 붙여야 한다~~ — ✅ 해소. `web/app.html`의 "New chat"이
   persona `learning`으로 이미 붙어 라이브 확인됨
